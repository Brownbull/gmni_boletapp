# Staging Environment Configuration
# Copy this to .env.staging and fill in your staging project credentials
#
# Usage:
#   npm run dev:staging    - Run dev server with staging Firebase
#   npm run build:staging  - Build for staging deployment
#
# To get credentials:
#   1. Go to https://console.firebase.google.com/project/boletapp-staging/settings/general
#   2. Scroll to "Your apps" → Web app
#   3. Copy the config values

# Firebase Staging Project Configuration
VITE_FIREBASE_API_KEY=your-staging-api-key
VITE_FIREBASE_AUTH_DOMAIN=boletapp-staging.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=boletapp-staging
VITE_FIREBASE_STORAGE_BUCKET=boletapp-staging.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your-staging-sender-id
VITE_FIREBASE_APP_ID=your-staging-app-id

# Gemini AI (can share with production or use separate key)
VITE_GEMINI_API_KEY=your-gemini-api-key

# Web Push VAPID Key (generate new one for staging)
VITE_VAPID_PUBLIC_KEY=your-staging-vapid-public-key

# E2E Mode - staging uses production mode (real Firebase, not emulator)
VITE_E2E_MODE=production

# Test account for staging (create a test user in staging Firebase Auth)
VITE_TEST_USER_EMAIL=staging-test@example.com
VITE_TEST_USER_PASSWORD=staging-test-password
