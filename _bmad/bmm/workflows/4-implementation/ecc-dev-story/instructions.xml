<workflow>
  <critical>The workflow execution engine is governed by: {project-root}/_bmad/core/tasks/workflow.xml</critical>
  <critical>You MUST have already loaded and processed: {installed_path}/workflow.yaml</critical>
  <critical>Communicate all responses in {communication_language} and language MUST be tailored to {user_skill_level}</critical>
  <critical>Generate all documents in {document_output_language}</critical>

  <critical>🎭 ECC-ORCHESTRATED STORY DEVELOPMENT - ECC Orchestrator spawns specialized ECC agents!</critical>
  <critical>This workflow uses the Task tool to spawn ECC agents for focused tasks</critical>
  <critical>Sequence: planner → tdd-guide → build-error-resolver → code-reviewer</critical>

  <!-- ECC ORCHESTRATOR PROTOCOL -->
  <orchestrator-protocol>
    <principle>Orchestrator spawns specialized ECC agents, coordinates work, synthesizes outputs</principle>
    <principle>Use Task tool with appropriate subagent_type for each ECC agent</principle>
    <principle>For parallel tasks, spawn multiple agents in a SINGLE message with multiple Task calls</principle>
    <principle>Create handoff documents between sequential agent phases</principle>
    <principle>Synthesize outputs after each agent completes</principle>

    <available-agents>
      | Agent | subagent_type | Purpose |
      |-------|---------------|---------|
      | Planner | everything-claude-code:planner | Implementation planning |
      | TDD Guide | everything-claude-code:tdd-guide | Test-first development |
      | Build Resolver | everything-claude-code:build-error-resolver | Fix build errors |
      | Code Reviewer | everything-claude-code:code-reviewer | Quality review |
      | Security Reviewer | everything-claude-code:security-reviewer | Security analysis |
    </available-agents>
  </orchestrator-protocol>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 0: Project Knowledge Loading                                       -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="0" goal="Load project knowledge, cache patterns and testing guidelines" tag="knowledge-init">
    <critical>📚 KNOWLEDGE CACHING: Load ALL project knowledge ONCE at session start, use throughout</critical>
    <critical>Load project patterns BEFORE orchestrating ECC agents</critical>

    <!-- STEP 0A: Load and cache project knowledge files -->
    <action>Load and cache {project-root}/_bmad/bmm/knowledge/code-review-patterns.md → {{cached_review_patterns}}</action>
    <action>Load and cache {project-root}/docs/architecture/firestore-patterns.md → {{cached_firestore}}</action>
    <action>Load and cache {project-root}/docs/architecture/state-management.md → {{cached_state_mgmt}}</action>
    <action>Load and cache {project-root}/docs/architecture/component-patterns.md → {{cached_components}}</action>
    <action>Store combined as {{project_patterns}} for passing to ECC agents</action>

    <!-- STEP 0B: Load and cache testing guidelines -->
    <action>Load and cache docs/architecture/TESTING-GUIDELINES.md → {{cached_testing_guidelines}}</action>
    <check if="story has E2E tasks or tests">
      <action>Load and cache tests/e2e/E2E-TEST-CONVENTIONS.md → {{cached_e2e_conventions}}</action>
    </check>
    <action>Store combined as {{project_testing_patterns}} for TDD Guide agents</action>

    <!-- STEP 0C: Generate session knowledge summary -->
    <action>Generate {{session_knowledge}} from cached files:
      {{session_knowledge}} = {
        patterns: summarized patterns from {{cached_review_patterns}} + {{cached_firestore}} + {{cached_state_mgmt}} + {{cached_components}},
        testing: {
          unit_max_lines: 300,
          integration_max_lines: 500,
          e2e_max_lines: 400,
          use_shared_factories: true,
          test_behavior_not_mocks: true,
          max_assertions_per_test: 5
        },
        e2e: {
          environment: 'staging_only',  // E2E tests run ONLY against staging, never local/emulator
          viewport: { width: 360, height: 780 },
          screenshots_required: true,
          cleanup_required: true,
          use_data_testid: true
        },
        must_check: key patterns from {{cached_review_patterns}} (git staging, sanitization, TOCTOU, batch ops, exports)
      }
    </action>

    <output>📚 **ECC Orchestrator Initialized — Project Knowledge Cached**

      Knowledge loaded and cached for session (no re-reads needed):
      - Code review patterns: {{review_patterns_summary}}
      - Firestore patterns: {{firestore_summary}}
      - State management: {{state_mgmt_summary}}
      - Component patterns: {{components_summary}}
      - Testing guidelines: cached (unit ≤300 LOC, integration ≤500 LOC, E2E ≤400 LOC)

      Session knowledge: {{session_knowledge}}
      ECC agents will receive cached context — no redundant file reads.
    </output>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 1: Find and Load Story                                             -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="1" goal="Find next ready story and load it" tag="story-discovery">
    <check if="{{story_path}} is provided">
      <action>Use {{story_path}} directly</action>
      <action>Read COMPLETE story file</action>
      <action>Extract story_key from filename or metadata</action>
      <goto anchor="task_check" />
    </check>

    <check if="{{sprint_status}} file exists">
      <action>Load the FULL file: {{sprint_status}}</action>
      <action>Find the FIRST story with status "ready-for-dev"</action>

      <check if="no ready-for-dev story found">
        <output>📋 No ready-for-dev stories found in sprint-status.yaml

          **Options:**
          1. Run `ecc-create-story` to create next story with ECC planning
          2. Specify a particular story file path
        </output>
        <ask>Choose option [1] or [2], or provide story path:</ask>
      </check>
    </check>

    <anchor id="task_check" />
    <action>Parse sections: Story, Acceptance Criteria, Tasks/Subtasks, Dev Notes</action>
    <action>Identify first incomplete task (unchecked [ ])</action>
    <action>Set {{story_context}} = summary of story requirements for ECC agents</action>

    <!-- ARCHITECTURE ENFORCEMENT: Load architectural ACs and documented patterns -->
    <critical>🏗️ ARCHITECTURE ENFORCEMENT: Extract architectural ACs and pattern documentation</critical>
    <action>Parse "Architectural Acceptance Criteria" section from story</action>
    <action>Extract {{file_location_acs}} - list of required file paths</action>
    <action>Extract {{pattern_acs}} - required patterns from documented architecture</action>
    <action>Extract {{antipattern_acs}} - things that must NOT happen per docs</action>
    <action>Parse "File Specification" table for {{expected_file_paths}}</action>
    <action>Extract {{architecture_reference}} - source of architectural patterns</action>

    <check if="architectural ACs found">
      <output>🏗️ **Architectural Requirements Loaded**

        **Architecture Source:** {{architecture_reference}}

        **File Location ACs:** {{file_location_ac_count}}
        **Pattern ACs:** {{pattern_ac_count}}
        **Anti-Pattern ACs:** {{antipattern_ac_count}}

        Implementation will be validated against documented patterns.
      </output>
    </check>

    <check if="NO architectural ACs found">
      <output>⚠️ **Warning: No Architectural ACs Found**

        This story was created without architectural acceptance criteria.
        This may lead to deviation from documented architecture.

        **Recommended Action:** Re-create story using `ecc-create-story` workflow
        to generate ACs from architecture documentation.
      </output>
      <ask>Continue without architectural validation? [Y/N]</ask>
    </check>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 2: ECC Planner - Implementation Planning                           -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="2" goal="Spawn ECC Planner for implementation planning" tag="ecc-planner">
    <critical>🎭 ECC ORCHESTRATOR: Spawning ECC Planner agent</critical>

    <action>Prepare planner context:
      - Story requirements: {{story_context}}
      - Architecture patterns: {{project_patterns}}
      - Incomplete tasks: {{task_list}}
    </action>

    <output>🎭 **Spawning ECC Planner...**

      Task: Create implementation plan for story {{story_key}}
      Context: Project patterns + story requirements
    </output>

    <ecc-spawn agent="planner">
      <task-call>
        subagent_type: "everything-claude-code:planner"
        description: "Plan story implementation"
        prompt: |
          Plan the implementation for story: {{story_key}}

          **Story Requirements:**
          {{story_context}}

          **Tasks to Implement:**
          {{task_list}}

          **Architecture Patterns:**
          {{project_patterns}}

          **Output Required:**
          1. Implementation approach for each task
          2. File changes required
          3. Dependencies and order
          4. Risk assessment
          5. Testing strategy alignment

          Create a clear implementation plan that the TDD Guide agent can follow.
      </task-call>
    </ecc-spawn>

    <action>Collect planner output as {{implementation_plan}}</action>
    <action>Store in Dev Agent Record → Implementation Plan</action>

    <output>✅ **ECC Planner Complete**

      Implementation plan created:
      {{implementation_plan_summary}}
    </output>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 2.5: Post-Planning Complexity Check                                -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="2.5" goal="Validate story size after planning reveals true scope" tag="post-planning-size">
    <critical>📏 MID-STORY SIZING: Check if planning revealed hidden complexity</critical>

    <action>Extract from {{implementation_plan}}:
      - {{planned_files}}: Number of files planner identified for changes
      - {{planned_subtasks}}: Subtasks inferred from plan
      - {{dependencies_discovered}}: New dependencies found during planning
    </action>

    <action>Compare to original story metrics:
      - {{original_files}}: From story File List / File Specification
      - {{original_subtasks}}: From story Tasks section
    </action>

    <action>Calculate growth:
      - {{file_growth}} = planned_files - original_files
      - {{subtask_growth}} = planned_subtasks - original_subtasks
    </action>

    <check if="file_growth > 3 OR subtask_growth > 5">
      <output>⚠️ **COMPLEXITY GROWTH DETECTED**

        **Original Story Estimate:**
        - Files: {{original_files}}
        - Subtasks: {{original_subtasks}}

        **After Planning:**
        - Files: {{planned_files}} (+{{file_growth}})
        - Subtasks: {{planned_subtasks}} (+{{subtask_growth}})

        **Growth Alert Thresholds:**
        - File growth > 3: {{file_growth > 3}}
        - Subtask growth > 5: {{subtask_growth > 5}}

        **Recommendation:** Consider splitting this story before proceeding.
      </output>

      <ask>How to proceed?
        [C]ontinue - Accept increased scope (document risk)
        [S]plit - Split story now before implementation
        [A]bort - Stop and re-scope with SM

        Choose [C], [S], or [A]:</ask>

      <check if="user chooses C">
        <action>Document in Dev Notes:
          - Complexity Growth Accepted: {date}
          - Original scope: {{original_files}} files, {{original_subtasks}} subtasks
          - Actual scope: {{planned_files}} files, {{planned_subtasks}} subtasks
          - Risk: May require extended implementation time
        </action>
        <output>⚠️ Proceeding with increased scope. Risk documented.</output>
      </check>

      <check if="user chooses S">
        <action>Invoke story-sizing workflow for this story</action>
        <action>Create split stories</action>
        <action>Update sprint-status.yaml with new stories</action>
        <action>Continue with first split story</action>
      </check>

      <check if="user chooses A">
        <action>Mark story status as "blocked" in sprint-status.yaml</action>
        <output>🛑 Story blocked for re-scoping. Run create-story to revise.</output>
        <stop/>
      </check>
    </check>

    <check if="file_growth &lt;= 3 AND subtask_growth &lt;= 5">
      <output>✅ Planning scope matches story estimate. Proceeding.</output>
    </check>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 3: Mark Story In-Progress                                          -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="3" goal="Mark story in-progress" tag="status-update">
    <check if="{{sprint_status}} file exists">
      <action>Update story status: ready-for-dev → in-progress</action>
      <output>🚀 Story {{story_key}} marked in-progress</output>
    </check>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 4: ECC TDD Guide - Test-First Implementation                       -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="4" goal="Spawn ECC TDD Guide for test-first implementation" tag="ecc-tdd">
    <critical>🎭 ECC ORCHESTRATOR: Spawning ECC TDD Guide agent for each task</critical>
    <critical>🏗️ ARCHITECTURE ENFORCEMENT: Validate file locations after each task</critical>
    <critical>🧪 TDD WATCH MODE: Start watcher once, cache files, avoid re-reads</critical>
    <critical>📝 BATCHED PROGRESS: Track subtasks in memory, write story file only on task_complete</critical>

    <!-- PROGRESS TRACKER: Initialize in-memory tracking to avoid per-subtask story edits -->
    <action>Initialize progress tracker in memory:
      {{progress_tracker}} = {
        completed_subtasks: [],
        completed_tasks: [],
        current_task: null,
        files_changed: []
      }
    </action>
    <output>📝 Progress tracker initialized. Story file updates batched at task boundaries.</output>

    <!-- SIZING METRICS: Track cumulative implementation size in memory -->
    <action>Initialize sizing metrics in memory:
      {{sizing_metrics}} = {
        current_files: 0,
        current_loc: 0,
        completed_task_count: 0,
        remaining_tasks: length of {{task_list}}
      }
    </action>

    <!-- FILE CACHING: Read once, use throughout -->
    <action>For each task's test + implementation file pair:
      - Read test file: {{test_file_path}} → cache as {{test_content}}
      - Read implementation file: {{impl_file_path}} → cache as {{impl_content}}
    </action>
    <output>📋 File content cached. No re-reads needed during TDD loop.</output>

    <!-- WATCH MODE: Start test watcher for continuous feedback -->
    <action>Start test watcher in background:
      npm test -- --watch {{test_file_path}}
    </action>
    <output>🔄 Test watcher started. File edits will auto-run tests.</output>

    <loop for="each incomplete task in {{task_list}}">
      <output>🎭 **Spawning ECC TDD Guide for Task: {{current_task}}**

        Phase: RED-GREEN-REFACTOR cycle
      </output>

      <ecc-spawn agent="tdd-guide">
        <task-call>
          subagent_type: "everything-claude-code:tdd-guide"
          description: "TDD implementation for {{current_task}}"
          prompt: |
            Implement task using strict TDD methodology: {{current_task}}

            **Implementation Plan (from Planner):**
            {{implementation_plan}}

            **Testing Patterns:**
            {{project_testing_patterns}}

            **Cached Testing Guidelines (from TESTING-GUIDELINES.md):**
            {{cached_testing_guidelines}}

            **Session Testing Constraints:**
            - Unit test max lines: {{session_knowledge.testing.unit_max_lines}}
            - Integration test max lines: {{session_knowledge.testing.integration_max_lines}}
            - E2E test max lines: {{session_knowledge.testing.e2e_max_lines}}
            - Use shared factories: {{session_knowledge.testing.use_shared_factories}}
            - Test behavior not mocks: {{session_knowledge.testing.test_behavior_not_mocks}}
            - Max assertions per test: {{session_knowledge.testing.max_assertions_per_test}}

            **E2E Conventions (if applicable):**
            - Viewport: {{session_knowledge.e2e.viewport.width}}x{{session_knowledge.e2e.viewport.height}}
            - Screenshots required: {{session_knowledge.e2e.screenshots_required}}
            - Cleanup required: {{session_knowledge.e2e.cleanup_required}}
            - Use data-testid: {{session_knowledge.e2e.use_data_testid}}

            ---

            ## 🚨 MANDATORY: Documented Architecture Compliance

            **Architecture Source:** {{architecture_reference}}

            **You MUST follow the documented architectural patterns.**
            Creating files outside documented locations creates technical debt.

            **Expected File Paths (from story File Specification):**
            {{expected_file_paths}}

            **Documented File Structure Patterns:**
            {{file_location_acs}}

            **Documented Pattern Requirements:**
            {{pattern_acs}}

            **Anti-Patterns from Documentation (MUST NOT DO):**
            {{antipattern_acs}}

            ---

            **TDD Watch Mode Active:**
            - Test watcher is running: edits auto-trigger test runs
            - Use cached file content (no re-reads): {{test_content}}, {{impl_content}}
            - After each edit, wait for watcher output instead of running tests manually
            - Update cached content in memory after each edit

            **Requirements:**
            1. Write FAILING tests first (RED phase)
            2. Implement MINIMAL code to pass tests (GREEN phase)
            3. Refactor while keeping tests green (REFACTOR phase)
            4. Ensure 80%+ coverage for new code
            5. **Create files ONLY at locations specified in story File Specification**
            6. **Follow all patterns documented in {{architecture_reference}}**
            7. **Use watcher output for test results (no separate npm test runs)**
            8. **Do NOT run npm run build or npx tsc per task — defer to Step 5 consolidated validation**
            9. **Do NOT edit the story file for subtask completion — report completed subtasks in your output, the orchestrator will batch-update**

            **Task Details:**
            {{current_task_details}}

            **Expected Output:**
            - Test files created/modified
            - Implementation files created/modified (AT SPECIFIED PATHS)
            - Coverage report
            - List of completed subtasks (for batch tracking, NOT written to story file)
        </task-call>
      </ecc-spawn>

      <action>Collect TDD output for {{current_task}}</action>

      <!-- ═══════════════════════════════════════════════════════════════════════ -->
      <!-- ARCHITECTURE VALIDATION: Check file locations after each task          -->
      <!-- ═══════════════════════════════════════════════════════════════════════ -->
      <action>Identify new files created by TDD Guide</action>
      <action>Validate each new file against {{expected_file_paths}} and architectural ACs</action>

      <check if="file location violation detected">
        <output>🚨 **ARCHITECTURAL VIOLATION DETECTED**

          **Violation Type:** File Location Mismatch

          **Expected:** {{expected_path}}
          **Actual:** {{actual_path}}

          **Affected AC:** {{violated_ac}}

          This violates the story's architectural requirements.
        </output>

        <ask>How to handle?
          [F]ix - Move file to correct location and update imports
          [E]xception - Document exception (requires justification)
          [A]bort - Stop implementation until resolved</ask>

        <check if="user chooses F">
          <action>Move file from {{actual_path}} to {{expected_path}}</action>
          <action>Update all imports referencing the old path</action>
          <action>Re-run tests to ensure nothing broke</action>
          <output>✅ File moved to correct location. Imports updated.</output>
        </check>

        <check if="user chooses E">
          <ask>Provide justification for exception:</ask>
          <action>Document exception in story Dev Notes:
            - Architectural exception for: {{actual_path}}
            - Expected: {{expected_path}}
            - Justification: {{user_justification}}
            - Date: {date}
          </action>
          <output>⚠️ Exception documented. Will be flagged in code review.</output>
        </check>

        <check if="user chooses A">
          <action>Mark story as blocked</action>
          <output>🛑 Implementation paused. Resolve architectural issue before continuing.</output>
          <stop/>
        </check>
      </check>

      <!-- Anti-pattern validation (based on documented anti-patterns) -->
      <check if="file violates any documented anti-pattern in {{antipattern_acs}}">
        <output>🚨 **ANTI-PATTERN VIOLATION**

          **Architecture Source:** {{architecture_reference}}

          **Issue:** File violates documented anti-pattern
          **Path:** {{actual_path}}

          **Anti-Pattern AC Violated:** {{violated_antipattern_ac}}

          **Documented Requirement:**
          {{antipattern_ac.description}}

          **Expected Location (per documented patterns):**
          {{expected_location_from_docs}}
        </output>

        <action>Move file to location per documented patterns</action>
        <action>Update imports</action>
      </check>

      <!-- Handle build errors with Build Resolver -->
      <check if="build errors detected">
        <output>⚠️ Build errors detected - spawning Build Resolver...</output>

        <ecc-spawn agent="build-resolver">
          <task-call>
            subagent_type: "everything-claude-code:build-error-resolver"
            description: "Fix build errors"
            prompt: |
              Fix the build/TypeScript errors with MINIMAL changes.

              **Errors:**
              {{build_errors}}

              **Rules:**
              - Fix ONLY what's needed to make build pass
              - Do NOT refactor unrelated code
              - Do NOT change architecture
              - Do NOT move files to different locations
              - Add type annotations where missing
              - Fix imports/exports
          </task-call>
        </ecc-spawn>
      </check>

      <!-- BATCHED PROGRESS: Track in memory, then single story file write per task -->
      <action>Add completed subtasks from TDD Guide output to {{progress_tracker}}.completed_subtasks</action>
      <action>Add {{current_task}} to {{progress_tracker}}.completed_tasks</action>
      <action>Add changed files to {{progress_tracker}}.files_changed</action>

      <!-- BATCH WRITE: Single story file edit for entire task (all subtasks + task checkbox) -->
      <action>BATCH EDIT story file:
        - Mark all subtasks in {{progress_tracker}}.completed_subtasks for this task as [x]
        - Mark {{current_task}} as [x] complete
        - Update File List with {{progress_tracker}}.files_changed
      </action>
      <action>Clear {{progress_tracker}}.completed_subtasks for next task</action>

      <!-- ═══════════════════════════════════════════════════════════════════════ -->
      <!-- MID-TASK SIZING CHECK: Alert if implementation exceeds thresholds      -->
      <!-- ═══════════════════════════════════════════════════════════════════════ -->
      <sizing-check tag="mid-task-size">
        <action>Update {{sizing_metrics}} from task output (in memory, no file reads):
          - Increment current_files with new files from this task
          - Add LOC from this task to current_loc
          - Increment completed_task_count
          - Decrement remaining_tasks
        </action>

        <check if="sizing_metrics.current_files > 8 OR sizing_metrics.current_loc > (500 * sizing_metrics.completed_task_count)">
          <output>⚠️ **MID-IMPLEMENTATION SIZE ALERT**

            **Task Completed:** {{current_task}}

            **Current Metrics:**
            - Files modified: {{sizing_metrics.current_files}} (limit: 8)
            - Lines added: {{sizing_metrics.current_loc}}
            - Remaining tasks: {{sizing_metrics.remaining_tasks}}

            **Projection:** At current rate, story will reach ~{{projected_files}} files and ~{{projected_loc}} LOC.

            **Risk:** Story may be too large for single implementation cycle.
          </output>

          <ask>Continue with remaining tasks?
            [C]ontinue - Accept current trajectory
            [P]ause - Commit current work, create follow-up story for remaining tasks
            [S]plit - Split remaining tasks into new story now

            Choose [C], [P], or [S]:</ask>

          <check if="user chooses C">
            <action>Document in Dev Notes:
              - Size Alert Accepted: {date}
              - Files: {{sizing_metrics.current_files}}, LOC: {{sizing_metrics.current_loc}}
              - Remaining tasks: {{sizing_metrics.remaining_tasks}}
            </action>
            <output>⚠️ Continuing. Size risk documented.</output>
          </check>

          <check if="user chooses P">
            <action>Update story: mark remaining tasks as "Deferred to follow-up"</action>
            <action>Create follow-up story with remaining tasks</action>
            <action>Add cross-reference between stories</action>
            <output>✅ Current work preserved. Follow-up story created for remaining tasks.</output>
            <action>Jump to Step 5 (validation) — skip remaining tasks in loop</action>
          </check>

          <check if="user chooses S">
            <action>Invoke story-sizing workflow with remaining tasks</action>
            <action>Create split stories</action>
            <action>Update sprint-status.yaml</action>
            <action>Continue with first split story</action>
          </check>
        </check>
      </sizing-check>

      <output>✅ **Task Complete:** {{current_task}}

        Files created (architecture validated):
        {{task_files_list}}
        Subtasks batched in single edit: {{batched_subtask_count}}
      </output>
    </loop>

    <!-- WATCH MODE: Stop watcher now that all tasks are complete -->
    <action>Stop test watcher (Ctrl+C or timeout after {{watch_timeout_minutes}} min)</action>
    <output>⏹️ Test watcher stopped.</output>

    <output>✅ **ECC TDD Guide Complete**

      Tasks implemented: {{completed_task_count}}
      Test coverage: {{coverage_percentage}}%

      **TDD Watch Mode Stats:**
      - File re-reads avoided: {{reads_saved}}
      - Separate test runs avoided: {{test_runs_saved}}

      **Batched Progress Stats:**
      - Story file edits: {{actual_story_edits}} (saved {{story_edits_saved}} vs per-subtask)
      - Sprint-status.yaml edits deferred to story completion (Step 7)

      **Architecture Validation:**
      - Files checked: {{files_validated_count}}
      - Violations fixed: {{violations_fixed_count}}
      - Exceptions documented: {{exceptions_count}}
    </output>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 5: Run Full Test Suite                                             -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="5" goal="Consolidated validation — build once, skip redundant type-checks" tag="validation">
    <critical>🔨 CONSOLIDATED VALIDATION: Single build covers type checking. No separate npx tsc --noEmit.</critical>

    <!-- 1. Run full test suite -->
    <action>Run full test suite: npm test or detected test command</action>

    <check if="tests fail">
      <output>❌ Tests failing - spawning Build Resolver...</output>

      <ecc-spawn agent="build-resolver">
        <task-call>
          subagent_type: "everything-claude-code:build-error-resolver"
          description: "Fix test failures"
          prompt: |
            Fix the failing tests with minimal changes.

            **Test Failures:**
            {{test_failures}}

            Fix only what's needed to make tests pass.
        </task-call>
      </ecc-spawn>

      <action>Re-run tests after fixes</action>
    </check>

    <!-- 2. Run lint once -->
    <action>Run linting: npm run lint or detected lint command</action>

    <!-- 3. Single consolidated build (includes TypeScript type checking) -->
    <action>Run build ONCE (includes type checking):
      npm run build 2>&amp;1 | tee build.log
    </action>

    <check if="build fails">
      <output>❌ Build failed - spawning Build Resolver...</output>

      <ecc-spawn agent="build-resolver">
        <task-call>
          subagent_type: "everything-claude-code:build-error-resolver"
          description: "Fix build/type errors"
          prompt: |
            Fix the build/TypeScript errors with MINIMAL changes.

            **Build Output:**
            {{build_errors}}

            **Rules:**
            - Fix ONLY what's needed to make build pass
            - Do NOT refactor unrelated code
            - Do NOT change architecture
        </task-call>
      </ecc-spawn>

      <action>Re-run build ONLY if changes were made</action>
    </check>

    <!-- Do NOT run separate type-check if build passed -->
    <check if="build passes">
      <output>✅ Build passed (includes TypeScript type checking — no separate tsc needed)</output>
    </check>

    <output>✅ **Consolidated Validation Complete**

      Tests: {{test_result}}
      Lint: {{lint_result}}
      Build: {{build_result}} (includes type checking)

      **Build commands saved:** No per-task builds, no separate tsc --noEmit
    </output>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 5.5: Pre-Review Final Size Check                                   -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="5.5" goal="Final size validation before review" tag="pre-review-size">
    <critical>📏 FINAL SIZE CHECK: Validate implementation size and capture metrics</critical>

    <action>Calculate final metrics from {{sizing_metrics}} and git diff:
      - {{total_files}}: All files created/modified
      - {{total_loc}}: Total lines added/changed
      - {{total_tests}}: Test file count and test case count
      - {{impl_loc}}: Implementation-only LOC (excluding tests)
      - {{test_loc}}: Test-only LOC
      - {{test_ratio}}: test_loc / impl_loc
    </action>

    <output>📏 **Pre-Review Size Report**

      **Implementation Metrics:**
      | Metric | Value | Limit | Status |
      |--------|-------|-------|--------|
      | Files | {{total_files}} | 8 | {{files_status}} |
      | LOC Added | {{total_loc}} | - | Info |
      | Impl LOC | {{impl_loc}} | - | Info |
      | Test LOC | {{test_loc}} | - | Info |
      | Tests | {{total_tests}} | - | Info |
      | Test Ratio | {{test_ratio}} | 0.3-0.5 | {{ratio_status}} |

      **Story Points:** {{original_points}} estimated → {{actual_complexity}} actual
    </output>

    <check if="total_files > 8">
      <output>⚠️ **File Count Exceeded:** {{total_files}} files (limit: 8).
        Consider breaking large commits into logical chunks for review.
      </output>
    </check>

    <check if="test_ratio > 1.0">
      <output>⚠️ **Test Ratio Warning:** Test code ({{test_loc}} LOC) exceeds implementation code ({{impl_loc}} LOC).
        Review tests for over-testing per TESTING-GUIDELINES.md:
        - Remove toHaveBeenCalled for pass-through functions
        - Reduce excessive mock setup
        - Test behavior, not implementation details
      </output>
    </check>

    <action>Record metrics in story Dev Notes:
      - Implementation Size: {{total_files}} files, {{total_loc}} LOC
      - Test Ratio: {{test_ratio}}
      - Estimated vs Actual: {{original_points}} → {{actual_complexity}}
    </action>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 6: Parallel Pre-Completion Review                                  -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="6" goal="Parallel ECC review before marking complete" tag="ecc-parallel-review">
    <critical>🎭 ECC ORCHESTRATOR: Spawning PARALLEL review agents (SINGLE message, MULTIPLE Task calls)</critical>

    <!-- ═══════════════════════════════════════════════════════════════════════ -->
    <!-- EXPLICIT PARALLEL EXECUTION DIRECTIVE                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════════ -->
    <parallel-execution-rule>
      🚨 HOW TO ACHIEVE TRUE PARALLELISM:

      You MUST issue BOTH Task tool invocations in your NEXT SINGLE RESPONSE.

      ❌ WRONG (Sequential - slow):
         Response 1: Task(code-reviewer) → wait → result
         Response 2: Task(security-reviewer) → wait → result

      ✅ CORRECT (Parallel - fast):
         Response 1: Task(code-reviewer) + Task(security-reviewer)
         → Both run simultaneously → Both results returned together

      IN YOUR NEXT MESSAGE, include BOTH Task tool calls at once.
    </parallel-execution-rule>

    <output>🎭 **Spawning Parallel Review Team...**

      Agents running in parallel:
      1. Code Reviewer - Quality and best practices
      2. Security Reviewer - Vulnerability analysis
    </output>

    <!-- CRITICAL: These must be in a SINGLE message for true parallelism -->
    <ecc-spawn-parallel>
      <task-call-1>
        subagent_type: "everything-claude-code:code-reviewer"
        description: "Code quality review"
        prompt: |
          Review the implementation for story: {{story_key}}

          **Files Changed:**
          {{file_list}}

          **Check for:**
          - Code quality and maintainability
          - Error handling
          - Performance issues
          - Test quality
          - Documentation

          Output: List of findings with severity (HIGH/MEDIUM/LOW)
      </task-call-1>

      <task-call-2>
        subagent_type: "everything-claude-code:security-reviewer"
        description: "Security review"
        prompt: |
          Security review for story: {{story_key}}

          **Files Changed:**
          {{file_list}}

          **Check for:**
          - OWASP Top 10 vulnerabilities
          - Hardcoded secrets
          - Input validation
          - Authentication/authorization issues

          Output: Security findings with severity and remediation
      </task-call-2>
    </ecc-spawn-parallel>

    <action>Collect outputs from both agents</action>
    <action>Merge findings by severity</action>

    <!-- Review Synthesis -->
    <output>🗺️ **Review Synthesis - Parallel Review Results**

      **Code Review Findings:**
      {{code_review_findings}}

      **Security Review Findings:**
      {{security_review_findings}}

      **Cross-Cutting Analysis:**
      - Workflow chain impacts: {{workflow_impacts}}
      - Architecture alignment: {{architecture_alignment}}
    </output>

    <check if="HIGH severity issues found">
      <action>Fix HIGH severity issues before proceeding</action>
      <action>Re-run affected tests</action>
    </check>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 6.5: Pre-Completion Architectural Validation                       -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="6.5" goal="Validate all architectural ACs before completion" tag="arch-validation">
    <critical>🏗️ ARCHITECTURE GATE: ALL architectural ACs must pass before completion</critical>

    <output>🏗️ **Pre-Completion Architectural Validation**

      **Architecture Source:** {{architecture_reference}}

      Checking all architectural acceptance criteria from documented patterns...
    </output>

    <action>Enumerate all files created/modified during implementation</action>
    <action>Validate each file location AC against actual file paths</action>
    <action>Validate pattern ACs from story (per documented architecture)</action>
    <action>Validate anti-pattern ACs from story (per documented architecture)</action>

    <validation-checks>
      <!-- File Location Validation (from story File Specification) -->
      <check type="file-location">
        <description>Verify all new files match story File Specification</description>
        <for-each file in="{{created_files}}">
          <compare expected="{{expected_file_paths}}" actual="{{file.path}}"/>
          <check if="mismatch">
            <flag severity="HIGH" ac="{{related_ac}}">
              File location mismatch per documented architecture:
              - Actual: {{file.path}}
              - Expected: {{expected_path}}
              - Source: {{architecture_reference}}
            </flag>
          </check>
        </for-each>
      </check>

      <!-- Pattern Compliance Validation (from documented patterns) -->
      <check type="documented-patterns">
        <description>Verify implementation follows documented patterns</description>
        <for-each pattern_ac in="{{pattern_acs}}">
          <validate ac="{{pattern_ac}}" against="{{created_files}}">
            <check if="violation">
              <flag severity="HIGH" ac="{{pattern_ac.id}}">
                Pattern violation per {{architecture_reference}}:
                - Required: {{pattern_ac.description}}
                - Actual: {{violation_details}}
              </flag>
            </check>
          </validate>
        </for-each>
      </check>

      <!-- Anti-Pattern Validation (from documented anti-patterns) -->
      <check type="documented-antipatterns">
        <description>Verify no documented anti-patterns introduced</description>
        <for-each antipattern_ac in="{{antipattern_acs}}">
          <scan for="{{antipattern_ac.pattern}}" in="{{created_files}}">
            <flag severity="HIGH" ac="{{antipattern_ac.id}}" if="found">
              Anti-pattern violation per {{architecture_reference}}:
              - Prohibited: {{antipattern_ac.description}}
              - Found in: {{file.path}}
            </flag>
          </scan>
        </for-each>
      </check>
    </validation-checks>

    <action>Compile validation results as {{arch_validation_results}}</action>

    <check if="all architectural ACs pass">
      <output>✅ **Architectural Validation PASSED**

        **Architecture Source:** {{architecture_reference}}

        All architectural acceptance criteria satisfied:
        {{passing_acs}}

        **Summary:**
        - File Location ACs: ✅ {{file_location_passed}}/{{file_location_total}}
        - Pattern ACs: ✅ {{pattern_passed}}/{{pattern_total}}
        - Anti-Pattern ACs: ✅ {{antipattern_passed}}/{{antipattern_total}}

        Implementation aligns with documented architecture.
      </output>
    </check>

    <check if="architectural AC failures detected">
      <output>🚨 **Architectural Validation FAILED**

        **Architecture Source:** {{architecture_reference}}

        The following architectural ACs (from documented patterns) are not satisfied:

        {{failing_acs}}

        **This BLOCKS story completion.**
        Deviation from documented architecture creates technical debt.
      </output>

      <ask>How to proceed?
        [F]ix - Fix violations now
        [E]xception - Document exceptions (requires strong justification)
        [A]bort - Stop and address issues manually</ask>

      <check if="user chooses F">
        <action>For each failing AC, apply fix (move files, update imports)</action>
        <action>Re-run validation</action>
      </check>

      <check if="user chooses E">
        <ask>For each failing AC, provide justification:</ask>
        <action>Document all exceptions in story file</action>
        <output>⚠️ Exceptions documented. Story will proceed with warnings.</output>
      </check>

      <check if="user chooses A">
        <output>🛑 Story completion blocked. Address architectural issues before proceeding.</output>
        <stop/>
      </check>
    </check>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 7: Story Completion                                                -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="7" goal="Mark story complete and update status" tag="completion">
    <action>Verify ALL tasks marked [x] complete</action>
    <action>Verify ALL functional acceptance criteria satisfied</action>
    <action>Verify ALL architectural acceptance criteria satisfied (from Step 6.5)</action>
    <action>Update story Status to "review"</action>

    <check if="{{sprint_status}} file exists">
      <action>Update sprint-status.yaml: {{story_key}} → review</action>
    </check>

    <output>✅ **Story Implementation Complete**

      Story: {{story_key}}
      Status: Ready for review
      Tasks completed: {{task_count}}
      Coverage: {{coverage_percentage}}%

      **ECC Agents Used:**
      - Planner: Implementation planning
      - TDD Guide: Test-first development
      - Build Resolver: Error fixing (if needed)
      - Code Reviewer: Quality review
      - Security Reviewer: Security analysis

      **Architectural Validation:**
      - File Location ACs: ✅ {{file_location_passed}}/{{file_location_total}}
      - Pattern ACs: ✅ {{pattern_passed}}/{{pattern_total}}
      - Anti-Pattern ACs: ✅ {{antipattern_passed}}/{{antipattern_total}}
      {{#if exceptions_count > 0}}
      - ⚠️ Exceptions documented: {{exceptions_count}} (will be reviewed in code review)
      {{/if}}
    </output>
  </step>

  <!-- ═══════════════════════════════════════════════════════════════════════ -->
  <!-- STEP 8: Next Steps                                                      -->
  <!-- ═══════════════════════════════════════════════════════════════════════ -->

  <step n="8" goal="Provide next steps after story completion" tag="next-steps">
    <output>**Next Steps:**
      - Run `ecc-code-review` for external review
      - Or proceed to deployment if approved
    </output>
  </step>

</workflow>
