/**
 * Feature: Settings
 *
 * Settings UI components and sub-views for user preferences, profile, and app configuration.
 * Story 15-4f: Feature module creation (Phase 4).
 *
 * Note: userPreferencesService and useUserPreferences are app-level shared
 * (used by scan, transactions, 5+ views) and remain in src/services/ and src/hooks/.
 */

export * from './components';
