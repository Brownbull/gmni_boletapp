/**
 * Re-export shim for backward compatibility.
 * Story 15b-1f: useBatchSession moved to @features/batch-review/hooks/
 */
export { useBatchSession } from '@features/batch-review/hooks/useBatchSession';
export type { BatchSession, UseBatchSessionReturn } from '@features/batch-review/hooks/useBatchSession';
