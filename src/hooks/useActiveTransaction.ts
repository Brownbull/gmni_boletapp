/**
 * Re-export shim — Story 15b-1c
 * Canonical location: @features/transaction-editor/hooks/useActiveTransaction
 */
export { useActiveTransaction, useActiveTransaction as default } from '@features/transaction-editor/hooks/useActiveTransaction';
export type {
  CreditOperationResult,
  UseActiveTransactionProps,
  UseActiveTransactionReturn,
} from '@features/transaction-editor/hooks/useActiveTransaction';
