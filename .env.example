# Firebase Configuration
# Get these values from Firebase Console > Project Settings > Your apps > Web app config
# https://console.firebase.google.com/

VITE_FIREBASE_API_KEY=AIzaXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890abcdef

# Gemini AI Configuration (used by Cloud Functions)
# Note: Client-side Gemini API is optional - server-side handles most AI processing
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Firebase Cloud Messaging VAPID Key (DEPRECATED - kept for backwards compatibility)
# Note: FCM VAPID key is no longer used since Story 14c.13 migrated to Web Push
VITE_FIREBASE_VAPID_KEY=your-fcm-vapid-key-here

# Web Push VAPID Public Key - Story 14c.13, Story 14c.15
# This key is used for native Web Push notifications (not FCM)
# Must match VAPID_PUBLIC_KEY in Cloud Functions .env
# Generate with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key-here

# ============================================================
# E2E Testing Configuration
# ============================================================
# Single variable controls both app Firebase connection AND test credentials:
#
# VITE_E2E_MODE=emulator (default):
#   - App connects to local Firebase emulators
#   - E2E tests use VITE_TEST_USER_* credentials
#   - Requires: npm run emulators
#
# VITE_E2E_MODE=production:
#   - App connects to production Firebase
#   - E2E tests use VITE_PROD_TEST_USER_* credentials
#   - For smoke tests against real Firebase

# E2E mode: 'emulator' (default) or 'production'
VITE_E2E_MODE=emulator

# Emulator test user (created automatically in emulator)
VITE_TEST_USER_EMAIL=e2e-test@example.com
VITE_TEST_USER_PASSWORD=e2e-test-password-123

# Production test account for smoke tests against real Firebase
VITE_PROD_TEST_USER_EMAIL=your-designated-test-account@gmail.com
VITE_PROD_TEST_USER_PASSWORD=your-production-test-password

# Set to 'true' to enable test login button in non-dev environments
# VITE_ENABLE_TEST_LOGIN=false

# ============================================================
# Environment Modes:
# ============================================================
# The app supports multiple environments:
#
# Development with Emulators (default): npm run dev
#   - Uses .env file with VITE_E2E_MODE=emulator
#   - Connects to local Firebase emulators
#   - Note: Chrome may have CORS issues; use Playwright tests
#
# Staging (real Firebase): npm run dev:staging
#   - Uses .env.staging file (copy from .env.staging.example)
#   - Connects to staging Firebase project (boletapp-staging)
#   - No CORS issues - good for manual browser testing
#   - Isolated from production data
#
# Production: npm run build && npm run preview
#   - Uses .env file with VITE_E2E_MODE=production
#   - Connects to production Firebase (boletapp-d609f)

# ============================================================
# Setup Instructions:
# ============================================================
# 1. Copy this file to .env (which is git-ignored)
# 2. Replace placeholder values with your actual credentials
# 3. Never commit .env to version control
# 4. Keep .env file secure and private
#
# For Firebase credentials:
#   - Go to https://console.firebase.google.com/
#   - Select your project
#   - Click the gear icon > Project settings
#   - Scroll down to "Your apps" section
#   - Click the web app (</>)  icon
#   - Copy the config values into this file
#
# For Gemini API key:
#   - Go to https://makersuite.google.com/app/apikey
#   - Click "Create API Key"
#   - Copy the generated key
#
# For Staging environment:
#   - Copy .env.staging.example to .env.staging
#   - Create boletapp-staging project in Firebase Console
#   - Register a web app and copy credentials to .env.staging
#   - Run: npm run dev:staging
