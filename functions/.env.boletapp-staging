# Staging-specific Cloud Functions environment (boletapp-staging project)
# Firebase loads this automatically when deploying with --project boletapp-staging

# Fixture mode: replaces Gemini API calls with deterministic fixtures (Plan B)
# NEVER set this in production .env — M1 guard blocks it anyway
SCAN_FIXTURE_MODE=true
