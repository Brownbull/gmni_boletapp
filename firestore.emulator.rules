rules_version = '2';

// EMULATOR ONLY RULES - Open access for development and testing
// WARNING: DO NOT use these rules in production!

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write for emulator development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
